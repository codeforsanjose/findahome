<div class="ui container">
  <div class="ui segment">
    <!-- TODO - search by radius -->
    <!-- TODO - fix responsive web design. Flexible columns so the information
    doesn't look too cramped on the side when the screen is half or quartersize
    -->
    <!-- <table class="ui definition table">

      <tr>
        <td class="one wide column">
            <div class="ui label">Rent:</div>
        </td>
        <td>
            {{range-slider
              defaultValue='500,1500'
              value='500,1500'
              min=1
              max=5000
              step=1
              tooltip='hide'
            }}
        </td>
      </tr>
    </table> -->

    <h3 class="ui horizontal divider header">
      Affordable Housing Units
    </h3>

    <!-- TODO Scrollable page -->
    <article class="listing-card">
      {{#each model as |listing|}}
        <i class="tag icon"></i>
        <a href={{listing.propertyWebsite}}><b>{{listing.propertyName}}</b></a>
        <p><i class="map pin icon"></i>{{listing.propertyAddress}}</p>
        <p><b>Type:</b> {{listing.propertyType}}</p>
        <p><b>Property Management:</b> {{listing.propertyManager}}</p>
        <p><i class="call icon"></i><b>Phone:</b>{{listing.propertyManagementPhone}}</p>
      {{/each}}
    </article>

  </div>
</div>
